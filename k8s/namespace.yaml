# =====================================================================
# Kubernetes Namespace
# Isolates all Portfolio resources from other cluster workloads
# =====================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: portfolio
  labels:
    app.kubernetes.io/name: portfolio
    app.kubernetes.io/managed-by: jenkins-pipeline
